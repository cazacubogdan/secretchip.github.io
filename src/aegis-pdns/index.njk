---
layout: base.njk
permalink: /aegis-pdns/
title: AEGIS PDNS
description: AEGIS PDNS is protective DNS that blocks risky domains before your devices connect — encrypted, verifiable, and deployed with help from SecretChip.
---

{% set eyebrow = "AEGIS PDNS · Protective DNS" %}
{% set heroTitle = "Block threats before they connect." %}
{% set heroLead = "Encrypted, policy-aware DNS that stops connections to known-risky domains — with public test domains so you can prove it's working." %}
{% set heroPrimaryLabel = "Get protected" %}
{% set heroPrimaryHref = "/contact/" %}
{% set heroSecondaryLabel = "Run the DNS test" %}
{% set heroSecondaryHref = "/aegis-pdns/test/" %}
{% include "page-hero.njk" %}

<section class="stack-xl section">
  <div>
    {% set eyebrow = "" %}{% set heading = "What you get" %}{% set asH1 = false %}{% set center = true %}{% set desc = "" %}
    {% include "section-heading.njk" %}
    <div class="grid3" style="margin-top: 24px;">
      <div class="card"><h3>Threats stopped early</h3><p>Risky and malicious domains are blocked at the DNS layer, before apps complete a connection.</p></div>
      <div class="card"><h3>Encrypted everywhere</h3><p>DoH, DoT, and DoQ for your devices, routers, browsers, and networks.</p></div>
      <div class="card"><h3>Proof, not promises</h3><p>Public canary domains and a live test page confirm exactly what's allowed and blocked.</p></div>
      <div class="card"><h3>Rollout with help</h3><p>We assist with policy design, deployment, false positives, and handover.</p></div>
      <div class="card"><h3>Transparent by design</h3><p>The filtering pipeline publishes manifests and hashes — nothing is hidden.</p></div>
      <div class="card"><h3>Private by default</h3><p>Logs serve operation and quality only. <strong>We never sell DNS query data.</strong></p></div>
    </div>
  </div>

  <div>
    {% set heading = "How it works" %}{% set center = true %}{% set desc = "" %}
    {% include "section-heading.njk" %}
    <div class="grid3" style="margin-top: 24px;">
      <div class="card"><h3>1 · Point your DNS</h3><p>Set your device, router, or network to the AEGIS resolver — no software to install.</p></div>
      <div class="card"><h3>2 · We filter</h3><p>Known-risky destinations are blocked using a curated, continuously improved list.</p></div>
      <div class="card"><h3>3 · You verify</h3><p>Use the test page and canary domains to confirm protection is live.</p></div>
    </div>
    <div class="btnrow" style="justify-content:center; margin-top: 20px;">
      <a class="btn" href="/aegis-pdns/setup/">Setup guide</a>
      <a class="btn" href="/aegis-pdns/test/">Run the DNS test</a>
    </div>
  </div>

  {% set center = true %}
  {% set ctaTitle = "Protect your team with AEGIS PDNS" %}
  {% set ctaText = "Talk to SecretChip about managed deployment, policy design, and rollout — for one network or thousands of endpoints." %}
  {% set ctaPrimaryLabel = "Talk to us" %}
  {% set ctaPrimaryHref = "/contact/" %}
  {% set ctaSecondaryLabel = "Explore vCISO" %}
  {% set ctaSecondaryHref = "/vciso/" %}
  {% include "cta.njk" %}
</section>
