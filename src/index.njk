---
layout: base.njk
title: false
description: SecretChip delivers AEGIS PDNS protective DNS — encrypted DNS filtering over DoH, DoT and DoQ that blocks malware, phishing and ransomware — plus vCISO virtual CISO leadership for SOC 2, ISO 27001, NIST CSF, GDPR, NIS2 and DORA compliance.
---

<section class="page-hero home-hero fade-up">
  <p class="eyebrow">Protective DNS &amp; Virtual CISO services</p>
  <h1>Security you can <span class="gradient-text">verify</span>. Leadership you can <span class="gradient-text">trust</span>.</h1>
  <p class="lead">Protective DNS you can test yourself — and virtual CISO leadership on demand. Built for EU teams meeting NIS2.</p>
  <div class="btnrow">
    <a class="btn primary" href="/contact/">Get in touch</a>
    <a class="btn" href="#aegis">See what we do</a>
  </div>

  <ul class="trust-row" aria-label="Why teams choose SecretChip">
    <li>EU-based</li>
    <li>NIS2-aligned</li>
    <li>Encrypted DoH/DoT/DoQ</li>
    <li>Never sell DNS data</li>
    <li>Free to test</li>
  </ul>
</section>

<section id="about" class="section">
  {% set eyebrow = "About SecretChip" %}{% set heading = "One partner, two ways to raise your bar." %}{% set center = true %}{% set desc = "" %}
  {% include "section-heading.njk" %}

  <div class="grid3" style="margin-top: 24px;">
    <div class="card"><h3>Clarity over noise</h3><p>Scope and tradeoffs, stated upfront.</p></div>
    <div class="card"><h3>Measurable controls</h3><p>Validated in real environments, not promised.</p></div>
    <div class="card"><h3>Deployment realism</h3><p>Built for the constraints you actually face.</p></div>
  </div>
</section>

<section id="aegis" class="band section">
  <div class="band-inner">
    {% set eyebrow = "AEGIS PDNS · Protective DNS for NIS2 cyber hygiene" %}{% set heading = "Block malware, phishing &amp; ransomware before they connect." %}{% set center = true %}{% set desc = "Encrypted DNS filtering over DoH, DoT and DoQ — a NIS2 cyber-hygiene control you can prove works." %}
    {% include "section-heading.njk" %}

    <ol class="steps" style="margin-top: 24px;">
      <li><span class="step-n">1</span><span class="step-t">Point your DNS</span><span class="step-d">Set our encrypted resolver — minutes, no agents.</span></li>
      <li><span class="step-n">2</span><span class="step-t">We filter</span><span class="step-d">Malicious domains blocked at lookup, around the clock.</span></li>
      <li><span class="step-n">3</span><span class="step-t">You verify</span><span class="step-d">Public test domains confirm exactly what's blocked.</span></li>
    </ol>

    <div class="grid4" style="margin-top: 24px;">
      <div class="card"><h3>Stopped early</h3><p>Risky domains blocked at the DNS layer.</p></div>
      <div class="card"><h3>Cyber-hygiene control</h3><p>Encrypted over HTTPS, TLS, and QUIC — NIS2 Art. 21 (g)(h).</p></div>
      <div class="card"><h3>Verify it yourself</h3><p>Public test domains confirm what's blocked.</p></div>
      <div class="card"><h3>Private by default</h3><p>We never sell DNS query data.</p></div>
    </div>

    <div class="stats" id="aegis-stats"
         data-stats-src="https://raw.githubusercontent.com/secretchip/AEGIS-DNS/refs/heads/main/public_dns_statistics/stats.txt"
         aria-label="AEGIS PDNS statistics"{% if not aegisStats.available %} hidden{% endif %}>
      <div class="stat"><span class="stat-num" data-stat="queries">{{ aegisStats.queries or "—" }}</span><span class="stat-label">Queries this week</span></div>
      <div class="stat"><span class="stat-num" data-stat="blocked">{{ aegisStats.blocked or "—" }}</span><span class="stat-label">Threats blocked</span></div>
      <div class="stat"><span class="stat-num" data-stat="blocklist">{{ aegisStats.blocklist or "—" }}</span><span class="stat-label">Domains on blocklist</span></div>
      <div class="stat"><span class="stat-num" data-stat="cache">{{ aegisStats.cache or "—" }}</span><span class="stat-label">Cache hit rate</span></div>
    </div>

    <p class="mini" style="text-align: center; margin: 18px auto 0;">
      <span class="mono">{{ site.endpoints.doh }}</span> ·
      <a href="/aegis.mobileconfig">Apple profile</a> ·
      <a href="{{ site.github.aegisDnsIssues }}" target="_blank" rel="noopener noreferrer">Report a domain</a>
    </p>
    <p class="mini" style="text-align: center; margin: 6px auto 0;">
      Built in the open on the <a href="{{ site.github.aegisDns }}" target="_blank" rel="noopener noreferrer">AEGIS-DNS project</a>.
    </p>

    <div class="btnrow" style="justify-content: center; margin-top: 18px;">
      <a class="btn primary" href="/contact/">Protect your team</a>
    </div>
  </div>
</section>

<section id="vciso" class="section">
  {% set eyebrow = "vCISO · NIS2-ready security leadership" %}{% set heading = "A CISO when you need one — not a headcount." %}{% set center = true %}{% set desc = "Meet NIS2, SOC 2 and ISO 27001 with virtual CISO leadership for strategy, governance and board accountability — part-time, interim, or ongoing." %}
  {% include "section-heading.njk" %}

  <ol class="steps" style="margin-top: 24px;">
    <li><span class="step-n">1</span><span class="step-t">Assess</span><span class="step-d">Gap analysis against NIS2 and your target frameworks.</span></li>
    <li><span class="step-n">2</span><span class="step-t">Plan</span><span class="step-d">A prioritised roadmap with owners and timelines.</span></li>
    <li><span class="step-n">3</span><span class="step-t">Operate</span><span class="step-d">We run the programme and report to your board.</span></li>
  </ol>

  <div class="grid4" style="margin-top: 24px;">
    <div class="card"><h3>Strategy &amp; roadmap</h3><p>From maturity assessment to a multi-year plan.</p></div>
    <div class="card"><h3>Compliance you can pass</h3><p>NIS2, DORA, SOC 2, ISO 27001, NIST CSF, GDPR, PCI DSS, and more.</p></div>
    <div class="card"><h3>NIS2 accountability</h3><p>Art. 20 management duties, Art. 21 measures, Art. 23 reporting.</p></div>
    <div class="card"><h3>Board-ready reporting</h3><p>Plain-language updates for leadership and auditors.</p></div>
  </div>

  <div class="btnrow" style="justify-content: center; margin-top: 22px;">
    <a class="btn primary" href="/contact/">Book a discovery call</a>
  </div>
</section>

{% if testimonials.length %}
<section id="testimonials" class="section">
  {% set eyebrow = "In their words" %}{% set heading = "Trusted by teams who verify." %}{% set center = true %}{% set desc = "" %}
  {% include "section-heading.njk" %}

  <div class="grid3" style="margin-top: 24px;">
    {% for t in testimonials %}
    <figure class="quote">
      <blockquote>{{ t.quote }}</blockquote>
      <figcaption>{{ t.name }}{% if t.role %}, {{ t.role }}{% endif %}{% if t.org %} · {{ t.org }}{% endif %}</figcaption>
    </figure>
    {% endfor %}
  </div>
</section>
{% endif %}

<section id="cta" class="band section">
  <div class="band-inner cta-final">
    <h2>Tell us what you're protecting.</h2>
    <p>We'll map a practical plan — and reply within two business days.</p>
    <div class="btnrow" style="justify-content: center;">
      <a class="btn primary" href="/contact/">Contact us</a>
    </div>
    <p class="mini" style="margin-top: 16px;">Still deciding? <a href="/faq/">Read the FAQ</a>.</p>
  </div>
</section>
